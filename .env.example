# API
# Базовый URL API (по умолчанию /api/ при проксировании)
# API_URL=/api/

# Прокси: /api -> backend (для Dancehub: http://localhost:8000)
# API_PROXY_PORT=8000
# или явно: VITE_API_PROXY_TARGET=http://localhost:8000
